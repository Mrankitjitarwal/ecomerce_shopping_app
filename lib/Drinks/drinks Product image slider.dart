import 'package:flutter/material.dart';
import 'package:flutter_image_slideshow/flutter_image_slideshow.dart';

class DrinksProduct_image_slider1 extends StatelessWidget {
  const DrinksProduct_image_slider1({super.key});

  @override
  Widget build(BuildContext context) {
    return
      ImageSlideshow(
          indicatorColor: Colors.redAccent,
          indicatorBackgroundColor: Colors.white,
          height:300,
          autoPlayInterval:3000,
          indicatorRadius:4,
          isLoop: true,
          children:[
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/cocacola.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/cocacola1.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/cocacola2.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/cocacola3.png'),),
          ]
      );
  }
}
class DrinksProduct_image_slider2 extends StatelessWidget {
  const DrinksProduct_image_slider2({super.key});

  @override
  Widget build(BuildContext context) {
    return ImageSlideshow(
        indicatorColor: Colors.redAccent,
        indicatorBackgroundColor: Colors.white,
        height:800,
        width: 500,
        autoPlayInterval:3000,
        indicatorRadius:4,
        isLoop: true,
        children:[
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/pepsi1.png'),),
          /*   Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/shoe1.png'),),*/
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/pepsi.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/pepsi1.png'),),
        ]
    );
  }
}
class DrinksProduct_image_slider3 extends StatelessWidget {
  const DrinksProduct_image_slider3({super.key});

  @override
  Widget build(BuildContext context) {
    return
      ImageSlideshow(
          indicatorColor: Colors.redAccent,
          indicatorBackgroundColor: Colors.white,
          height:300,
          autoPlayInterval:3000,
          indicatorRadius:4,
          isLoop: true,
          children:[
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/tums1.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/thumsup2.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up.png'),),
          ]
      );
  }
}
class DrinksProduct_image_slider4 extends StatelessWidget {
  const DrinksProduct_image_slider4({super.key});

  @override
  Widget build(BuildContext context) {
    return
      ImageSlideshow(
          indicatorColor: Colors.redAccent,
          indicatorBackgroundColor: Colors.white,
          height:300,
          autoPlayInterval:3000,
          indicatorRadius:4,
          isLoop: true,
          children:[
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up Charge.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up Charge.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up Charge.png'),),
            Padding(padding: EdgeInsets.all(8),
              child: Image.asset('images/Thums Up Charge.png'),),
          ]
      );
  }
}







class DrinksProduct_image_slider5 extends StatelessWidget {
  const DrinksProduct_image_slider5({super.key});

  @override
  Widget build(BuildContext context) {
    return ImageSlideshow(
        indicatorColor: Colors.redAccent,
        indicatorBackgroundColor: Colors.white,
        height:800,
        width: 500,
        autoPlayInterval:3000,
        indicatorRadius:4,
        isLoop: true,
        children:[
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Yes.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Yes.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Yes.pngg'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Yes.png'),),
        ]
    );
  }
}





class DrinksProduct_image_slider6 extends StatelessWidget {
  const DrinksProduct_image_slider6({super.key});

  @override
  Widget build(BuildContext context) {
    return ImageSlideshow(
        indicatorColor: Colors.redAccent,
        indicatorBackgroundColor: Colors.white,
        height:800,
        width: 500,
        autoPlayInterval:3000,
        indicatorRadius:4,
        isLoop: true,
        children:[
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Moutn DEW.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Moutn DEW2.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Moutn DEW1.png'),),
          Padding(padding: EdgeInsets.all(8),
            child: Image.asset('images/Moutn DEW2.png'),),
        ]
    );
  }
}






